# Review package: initial commit

## Commits
b96185d feat: ????獢?Docker?蔭

## Files changed
 Dockerfile         | 18 ++++++++++++++++++  docker-compose.yml | 11 +++++++++++  package.json       | 22 ++++++++++++++++++++++  4 files changed, 68 insertions(+)

## Diff
new file mode 100644 index 0000000..e602539 --- /dev/null +++ b/.gitignore @@ -0,0 +1,17 @@ +# dependencies +node_modules/ + +# next.js +.next/ + +# environment +.env.local + +# logs +*.log + +# data +data/ + +# superpowers +.superpowers/ diff --git a/Dockerfile b/Dockerfile new file mode 100644 index 0000000..f51de0a --- /dev/null +++ b/Dockerfile @@ -0,0 +1,18 @@ +# Stage 1: Build +FROM node:18-alpine AS builder +WORKDIR /app +COPY package*.json ./ +RUN npm ci +COPY . . +RUN npm run build + +# Stage 2: Production +FROM node:18-alpine AS runner +WORKDIR /app +ENV NODE_ENV=production +COPY --from=builder /app/.next ./.next +COPY --from=builder /app/node_modules ./node_modules +COPY --from=builder /app/package.json ./package.json +COPY --from=builder /app/public ./public +EXPOSE 3000 +CMD ["npm", "start"] diff --git a/docker-compose.yml b/docker-compose.yml new file mode 100644 index 0000000..c467a07 --- /dev/null +++ b/docker-compose.yml @@ -0,0 +1,11 @@ +version: "3.8" + +services: +  app: +    build: . +    ports: +      - "3000:3000" +    environment: +      - NODE_ENV=production +    volumes: +      - ./data:/app/data diff --git a/package.json b/package.json new file mode 100644 index 0000000..6ff9603 --- /dev/null +++ b/package.json @@ -0,0 +1,22 @@ +{ +  "name": "stock-analysis-platform", +  "version": "0.1.0", +  "private": true, +  "scripts": { +    "dev": "next dev", +    "build": "next build", +    "start": "next start", +    "test": "jest" +  }, +  "dependencies": { +    "next": "latest", +    "react": "latest", +    "react-dom": "latest", +    "recharts": "^2.12.0" +  }, +  "devDependencies": { +    "jest": "^29.7.0", +    "@testing-library/react": "^14.2.0", +    "@testing-library/jest-dom": "^6.4.0" +  } +}
